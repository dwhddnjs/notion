"use client"

import React from "react"

interface NavbarProps {
  isCollapsed: boolean
  onRestWidth: () => void
}
export const Navbar = ({ isCollapsed, onRestWidth }: NavbarProps) => {
  return <div>Navbar</div>
}
